# Manifests

Raw Kubernetes YAML manifests, no Helm chart, no operator.

- `prometheus/` : Prometheus deployment, service, config
- `alertmanager/` : Alertmanager deployment, service, config
- `grafana/` : Grafana deployment, service, datasources
- `exporters/` : node-exporter and kube-state-metrics
