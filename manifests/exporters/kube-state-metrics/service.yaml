apiVersion: v1
kind: Service
metadata:
  namespace: monitoring
  name: kube-state-metrics
spec:
  selector:
    app: kube-state-metrics
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
